<% if params[:action] == "home" && user_signed_in? == false %>
<% else %>

    <div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
      <%= link_to root_path, class: "navbar-brand" do %>
        <i class="fas fa-landmark"></i>
        <% end %>
      <% unless params[:action] == "home" %>
        <%= form_tag results_deputies_path, class:"d-flex form-navbar", method: :get do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            id: "location-search",
          placeholder: "Tapez votre adresse..."
        %>
          <%= submit_tag "Search", class: "btn btn-primary ml-2" %>
        <% end %>
      <% end %>


      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <% if user_signed_in? %>
            <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete %>
            <!-- <li class="nav-item dropdown">
                <p class="dropdown-toggle clickable hidden" id="navbarDropdown" data-toggle="dropdown" aria-expanded="false">
                  Mes députés
                </p>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">

                </div>
            </li> -->
          <% else %>
            <li class="nav-item">
              <%= link_to "Se connecter", new_user_session_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "S'inscrire", new_user_registration_path, class: "nav-link button-primary-empty" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
<% end %>
